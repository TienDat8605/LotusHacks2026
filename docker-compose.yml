name: lotus-hacks

services:
  caddy:
    image: caddy:2-alpine
    init: true
    environment:
      APP_DOMAIN: "${APP_DOMAIN:-localhost}"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - frontend
      - backend
      - ai-service
    restart: unless-stopped

  ai-service:
    build:
      context: ./backend/services
      dockerfile: Dockerfile.dev
    image: lotus-hacks-ai-service:dev
    init: true
    working_dir: /workspace/backend/services
    environment:
      PYTHONUNBUFFERED: "1"
      AI_SERVICE_HOST: 0.0.0.0
      AI_SERVICE_PORT: "8090"
      AI_CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
      AI_REVIEW_DATA_PATH: ../../data/data.json
      AI_EMBEDDED_REVIEW_PATH: ../../data/review_embeddings.json
      UGC_DATASET_PATH: ../../data/data.json
      UGC_STORAGE_PATH: ../../data/ugc_videos
      UGC_JOBS_PATH: ../../data/ugc_jobs
    ports:
      - "8090:8090"
    volumes:
      - ./backend/services:/workspace/backend/services
      - ./data:/workspace/data
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    image: lotus-hacks-backend:dev
    init: true
    working_dir: /workspace/backend
    environment:
      PORT: "8080"
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
      POI_DATA_PATH: ../data/data.json
      UGC_SERVICE_URL: http://ai-service:8090
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/workspace/backend
      - ./data:/workspace/data
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
    restart: unless-stopped

  frontend:
    image: node:20-alpine
    init: true
    working_dir: /workspace/frontend
    command: >
      sh -lc "if [ ! -f node_modules/.lock.sha ] || ! sha256sum -c node_modules/.lock.sha >/dev/null 2>&1; then
      npm ci &&
      sha256sum package-lock.json > node_modules/.lock.sha; fi &&
      npm run dev -- --host 0.0.0.0 --port 5173"
    environment:
      VITE_API_MODE: real
      VITE_API_BASE_URL: "${VITE_API_BASE_URL:-}"
      VITE_AI_API_BASE_URL: "${VITE_AI_API_BASE_URL:-}"
      VITE_UGC_API_BASE_URL: "${VITE_UGC_API_BASE_URL:-}"
      APP_DOMAIN: "${APP_DOMAIN:-localhost}"
      VITE_ALLOWED_HOSTS: "${VITE_ALLOWED_HOSTS:-}"
      CHOKIDAR_USEPOLLING: "${CHOKIDAR_USEPOLLING:-false}"
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/workspace/frontend
      - frontend-node-modules:/workspace/frontend/node_modules
    restart: unless-stopped

volumes:
  caddy-data:
  caddy-config:
  go-mod-cache:
  go-build-cache:
  frontend-node-modules:
